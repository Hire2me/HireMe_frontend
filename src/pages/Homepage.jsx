import React from "react"


const HomePage = () => {
    return (
        <div> 
            git init
            git add . 
            git commit -m 'message'
            git pull/push origin development
        </div>
    )
}

export default HomePage